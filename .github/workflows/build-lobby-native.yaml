name: "Build Lobby (native)"

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'messaging/**'
      - 'core/**'
      - '.github/workflows/build-lobby-native.yaml'
      - 'lobby/**'
      - 'build.gradle.kts'

jobs:
  build:
    runs-on: "ubuntu-latest"
    steps:
      - uses: "actions/checkout@v4"

      - uses: "graalvm/setup-graalvm@v1"
        with:
          java-version: "25"
          distribution: "graalvm-community"
          github-token: "${{ secrets.GITHUB_TOKEN }}"
          cache: "gradle"
          native-image-job-reports: "true"

      - uses: "emortalmc/actions/java/build@main"
        with:
          token: "${{ secrets.GITHUB_TOKEN }}"
          project: "lobby-native"
          gradle-command: "./gradlew lobby:nativeCompile --no-daemon"
          dockerfile-path: './lobby/Dockerfile.native'
          dockerfile-context: './lobby/'
